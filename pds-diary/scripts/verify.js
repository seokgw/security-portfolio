require('dotenv').config();const pool=require('../src/db');
(async()=>{try{const [[health]]=await pool.query("SELECT DATABASE() db,@@session.time_zone timezone");const [[counts]]=await pool.query(`SELECT (SELECT COUNT(*) FROM plans) plans,(SELECT COUNT(*) FROM tasks WHERE deleted_at IS NULL) tasks,(SELECT COUNT(*) FROM execution_logs) execution_logs`);console.log(JSON.stringify({connection:'ok',...health,...counts},null,2));if(counts.plans<1||counts.tasks<5||counts.execution_logs<3)throw new Error('최소 실제 데이터 조건을 충족하지 않습니다. sql/seed.sql을 MariaDB에 적용하세요.');console.log('MariaDB 실제 데이터 검증 통과');}catch(e){console.error('검증 실패:',e.message);process.exitCode=1}finally{await pool.end()}})();
